@model IEnumerable<lab_7.Models.Evaluation_criteria>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Criteria_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Criteria_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Criteria_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Criteria_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Criteria_id })
        </td>
    </tr>
}

</table>
