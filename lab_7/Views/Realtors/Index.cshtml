@model IEnumerable<lab_7.Models.Realtor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Last_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.First_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Middle_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Contact_phone)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Last_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.First_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Middle_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Contact_phone)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Realtor_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Realtor_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Realtor_id })
        </td>
    </tr>
}

</table>
